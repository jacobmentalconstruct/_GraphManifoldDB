# BPE-SVD Embedder Demo — Dependencies
# Installed into _showcase/.venv (isolated from parent project)

numpy>=1.24.0

# The bpe-svd package is vendored from the local packages/ directory.
# It is installed here so the provider is available when the demo is
# wired up.  The showcase stubs (core.py) do NOT import from it yet.
# -e ../packages/bpe_svd is added by install.bat/install.sh directly
# so pip can resolve it as a local editable install.
